import InstallationContent from "@site/src/components/DocsComponents/Installation/InstallationContent";

# Installation

export const Installation = ({ children }) => {
  return (
    <span
      style={{
        borderRadius: "2px",
        padding: "0.2rem",
      }}
    >
      {children}
    </span>
  );
};

<Installation>
  <h3>Installation Guide</h3>
  <InstallationContent />
</Installation>
